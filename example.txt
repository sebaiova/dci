PROGRAM IOTest;
VAR X, Y : INTEGER;
    a : boolean;

BEGIN
    WRITE(A);
    
    READ(Y); 
    
    WRITE(X);
    Y := 10;

    IF X > Y THEN
        WRITE(X - Y)
    ELSE
        WRITE(Y - X);
END.